@page
@model FizzBuzz.Pages.Zapisane_w_sesjiModel
    <h3>Zapisane w sesji</h3>
    @{
       
        @for (int i = Model.listOfNumbers.Count - 1; i >= 0; i--)
        {
            <p> Liczba: @Model.listOfNumbers[i].Number  Wiadomość: @Model.listOfNumbers[i].Message Data: @Model.listOfNumbers[i].Date</p>

        }
    }
